# Story 01.06d — Calls (deletar wbotMonitor após handleCall pronto)

## Status
**Draft** — depende de **01.03 Done** (handleCall implementado).

## Story
**As a** sistema Atendechat
**I want** remover `wbotMonitor.ts` (que escutava calls via Baileys WebSocket) após confirmar que `handleCall.ts` (webhook) cobre o caso
**So that** não fique código morto duplicando funcionalidade.

## Acceptance Criteria
1. `handleCall.ts` (criado em 01.03) cobre o caso de **chamada recebida** com:
   - Mensagem automática "voz/vídeo desabilitado" enviada ao remetente.
   - Criação de `Message` com `mediaType="call_log"` no ticket.
   - Reabertura de ticket fechado se aplicável.
   - Comportamento idêntico ao `wbotMonitor.ts:35-110` atual.
2. Smoke test: ligar para a instância sandbox em modo voz, validar:
   - Mensagem automática chega ao chamador.
   - Ticket é criado/atualizado com call_log.
3. Após validação, **deletar** `services/WbotServices/wbotMonitor.ts` e remover sua referência em `StartWhatsAppSession.ts` (linha que chama `wbotMonitor(wsocket, whatsapp, companyId)`).
4. Build limpo.

## Tasks / Subtasks
- [ ] Smoke test do handleCall (chamada real recebida)
- [ ] Deletar `wbotMonitor.ts`
- [ ] Remover import e chamada em `StartWhatsAppSession.ts`
- [ ] Validar que multi-language funciona (pt/en/es) — `wbotMonitor.ts:48` tem mapa de mensagens

## Dev Notes

### Trecho a migrar (referência)
A lógica de `wbotMonitor.ts:43-110` (terminate da call) precisa estar em `handleCall.ts`. Confirmar que:
- `Setting` para `companyId/key=call` é consultado.
- Mensagem traduzida pelo `Company.language`.
- Contact/Ticket lookup correto.
- `mediaType: "call_log"` preservado.

⚠️ **Risco:** se `handleCall` estiver incompleto, esta story bloqueia até completarmos. Validar com Carlos antes de deletar wbotMonitor.

## Testing
- E2E manual com chamada real.
- Multi-language: testar com Company.language=en.

## File List
*A ser preenchido pelo Dex.*

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-05-09 | 0.1 | Draft (fatiada de 01.06) | Pax |
